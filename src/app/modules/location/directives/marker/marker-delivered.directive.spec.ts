import { MarkerDeliveredDirective } from './marker-delivered.directive';

describe('MarkerDeliveredDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkerDeliveredDirective();
    expect(directive).toBeTruthy();
  });
});
